[package]
name    = "gguf-core"
version.workspace      = true
edition.workspace      = true
license.workspace      = true
rust-version.workspace = true
description = "Device-agnostic types and the backend trait for the gguf-rs inference engine"

[dependencies]
half = { workspace = true }
